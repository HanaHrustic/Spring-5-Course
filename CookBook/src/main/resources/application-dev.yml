spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ag04_dev
    username: ag04_dev_user
    password: hana
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#  properties:
#    javax:
#      persistence:
#        schema-generation:
#          create-source: metadata
#          scripts:
#            action: create
#            create-target: ag04_database_create.sql
  sql:
    init:
      mode: always
      platform: mysql